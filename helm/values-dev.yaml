replicaCount: 1

global:
  platform: aws
  environment: dev

frontend:
  service:
    type: ClusterIP

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
